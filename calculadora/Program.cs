//paso 0: declarar las variables que vamos a usar
// debemos aclarar que tipo de dato será la variable (int, float, bool, char)
// int n1;
// int n2;
// int resultado;

// // paso 1: pedir valores.
// Console.WriteLine("Ingrese un número: ");
// // para asignar la variable a lo que ingresa el usuario
// // el console.readline puede recibir cualquier tipo de dato, por lo que hay que arreglarlo sino va a dar error
// // se arregla agregándole int.parse

// n1 = int.Parse(Console.ReadLine());
// Console.WriteLine("Ingrese otro: ");
// n2 = int.Parse(Console.ReadLine());

// //paso 2: realizar cálculo.
// //cuando creo la variable resultado acá, dice que no existe asi que tenemos que declararla al principio.

// resultado = n1 + n2;

// //paso 3: emitir resultado.

// Console.WriteLine("El resultado es: " + resultado);
